package sort;

/**
 * User: Oleg_Kariakin
 * Date: 3/28/16
 * <p/>
 * When you don't need a stable sort and average case performance matters more
 * than worst case performance. A quick sort is O(N log N) on average, O(N^2) in the worst case.
 * A good implementation uses O(log N) auxiliary storage in the form of stack space for recursion.
 */
public class QuickSort implements Sort {

    @Override
    public int[] sort(int[] arrayToSort, boolean ascending) throws IllegalArgumentException {
        return new int[0];  //To change body of implemented methods use File | Settings | File Templates.
    }
}

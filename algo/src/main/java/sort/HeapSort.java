package sort;

/**
 * User: Oleg_Kariakin
 * Date: 3/28/16
 * <p/>
 * When you don't need a stable sort and you care more about worst case performance than average case performance.
 * It's guaranteed to be O(N log N), and uses O(1) auxiliary space, meaning that you won't unexpectedly run out
 * of heap or stack space on very large inputs.
 */
public class HeapSort implements Sort {

    @Override
    public int[] sort(int[] arrayToSort, boolean ascending) throws IllegalArgumentException {
        return new int[0];  //To change body of implemented methods use File | Settings | File Templates.
    }
}
